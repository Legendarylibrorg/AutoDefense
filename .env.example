AUTODEFENSE_LOG_LEVEL=INFO
AUTODEFENSE_ENVIRONMENT=local
# Outside local: AUTODEFENSE_API_KEY is required at startup. With data encryption on, AUTODEFENSE_DATA_KEY_B64 is also required.
# For production, staging, or prod: AUTODEFENSE_SCANNER_HMAC_KEY is required at startup; without it outside local, POST /scan/kernel returns 503.
AUTODEFENSE_SELF_HEAL_ENABLED=true

# API key for authenticating dashboard and programmatic access.
# Auto-generated by scripts/start.sh if left empty.
AUTODEFENSE_API_KEY=

# HMAC key for scanner payload signing (shared between scanner + backend).
# Auto-generated by scripts/start.sh if left empty.
AUTODEFENSE_SCANNER_HMAC_KEY=

# Redis password (used by both Redis and backend).
AUTODEFENSE_REDIS_PASSWORD=

# Data encryption (at-rest in Redis, v2 envelope: 2× AES-256-GCM + HMAC + SHA-256 over canonical JSON)
# One 32-byte master → three HKDF-SHA256 subkeys (inner, outer, HMAC). See docs/security.md#encryption.
AUTODEFENSE_DATA_ENCRYPTION_ENABLED=true
AUTODEFENSE_DATA_KEY_B64=

# Sealed transport (browser → backend for /analyze/sealed, /scan/sealed)
# Same three-subkey HKDF scheme; value must match VITE_TRANSPORT_KEY_B64 when using the dashboard.
AUTODEFENSE_TRANSPORT_SEAL_ENABLED=true
AUTODEFENSE_TRANSPORT_KEY_B64=

# CORS origins (comma-separated, change for production)
AUTODEFENSE_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Browser-facing backend URLs (dashboard CSP connect-src; defaults match docker-compose)
AUTODEFENSE_PUBLIC_BACKEND_HTTP=http://localhost:8000
AUTODEFENSE_PUBLIC_BACKEND_WS=ws://localhost:8000

# Trust X-Forwarded-For for rate-limit client IP (leftmost address). 0 = use TCP peer only.
AUTODEFENSE_TRUSTED_PROXY_HOPS=0
