# Supply-chain defaults for local installs and CI (npm ci reads this file).
audit-level=moderate
fund=false
engine-strict=true
# Reduce install-time script execution (CI passes --ignore-scripts explicitly).
ignore-scripts=true
